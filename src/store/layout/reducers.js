import {fa} from '../../languages';

const layout = (state = {direction: 'rtl', language: 'fa', data: {...fa}}, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default layout;